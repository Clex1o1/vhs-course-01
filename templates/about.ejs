<!-- Bootstrap-Karte für ein schönes Layout -->
<div class="card">
  <div class="card-body">
    <!-- Titel der Seite - wird dynamisch von der Route gesetzt -->
    <h1 class="card-title"><%= title %></h1>
    <!-- Inhalt der Seite - wird dynamisch von der Route gesetzt -->
    <div class="card-text"><%= content %></div>
  </div>
</div>
